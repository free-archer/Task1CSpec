---
description: Запустить этап разработчика Task1CSpec и создать Реализация.md; код менять только после утверждения.
argument-hint: "<номер-задачи>"
allowed-tools: [Read, Glob, Grep, Bash, Write, Edit]
---

# TaskDeveloper

Пользователь вызвал `/TaskDeveloper` с аргументами: `$ARGUMENTS`

Прочитай `skills/task1cspec/SKILL.md`, выбери роль `Разработчик`, прочитай все существующие спецификации этой задачи, создай `Реализация.md` и остановись на проверку пользователем. Меняй программный код только после явного утверждения спецификации реализации.
